#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAXN = 1e5 + 5;

int main(){
	ios::sync_with_stdio(false);
	int q;
	cin >> q;
	ll a,b,c;
	while(q--){
		cin >> a >> b >> c;
		ll ans;
		ans=a+b+c;
		ans= ans/2;
		cout << ans << endl;
	}
	return 0;
}
